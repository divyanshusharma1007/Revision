# os module in python 
# this module is used to work with the operating system 
import os 
os.mkdir("data") #used to create the directory 
os.path.exists("problem") # check that the folder exists or not 
os.remove("problems") #used to remove the directory of file 
os.chdir("path") #used to change the working directory 
os.listdir() #used to list all directories avilabe in folder 
# explore the os module 
os.system()#run you own commands 
os.cwd()