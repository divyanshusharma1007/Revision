# walrus operator is used to assign the value to a variable with in a expression 
# a:=5


# shutil module-> this module is used to perfrm the high level file operations in python program 

# request modyule 
import requests
# request.get("url",headers={},json={})
requests.post("url",headers={},json={})